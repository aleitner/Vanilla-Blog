<%= form_for(@comment) do |f| %>
  <% if @comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comment.errors.count, "error") %> prohibited this comment from being saved:</h2>

      <ul>
      <% @comment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= current_user.user_name %>
  </div>

  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area  :content %>
  </div>

  <div class="field">
    <%if params[:blogpost_id] == nil%>
      <%= f.hidden_field :blogpost_id, :value => @blogpost_id %><br>
    <%else%>
      <%= f.hidden_field :blogpost_id, :value => params[:blogpost_id] %><br>
    <%end%>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
